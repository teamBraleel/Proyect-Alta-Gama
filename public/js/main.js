window.addEventListener('scroll', function()
{
    let animacion = document.getElementById('animado');
    let poci1 = animacion.getBoundingClientRect().top;
    console.log(poci1);
    let tamañopantalla = window.innerHeight/1;

    if(poci1 < tamañopantalla)
    {
        animacion.style.animation = 'mover 3s ease-out'
    }

});
window.addEventListener('scroll', function()
{
    let animacion = document.getElementById('animado1');
    let poci1 = animacion.getBoundingClientRect().top;
    console.log(poci1);
    let tamañopantalla = window.innerHeight/1;

    if(poci1 < tamañopantalla)
    {
        animacion.style.animation = 'mover 3s ease-out'
    }

});
window.addEventListener('scroll', function()
{
    let animacion = document.getElementById('animado2');
    let poci1 = animacion.getBoundingClientRect().top;
    console.log(poci1);
    let tamañopantalla = window.innerHeight/1;

    if(poci1 < tamañopantalla)
    {
        animacion.style.animation = 'mover 3s ease-out'
    }

});
window.addEventListener('scroll', function()
{
    let animacion = document.getElementById('animado3');
    let poci1 = animacion.getBoundingClientRect().top;
    console.log(poci1);
    let tamañopantalla = window.innerHeight/1;

    if(poci1 < tamañopantalla)
    {
        animacion.style.animation = 'mover 3s ease-out'
    }

});
window.addEventListener('scroll', function()
{
    let animacion = document.getElementById('animado4');
    let poci1 = animacion.getBoundingClientRect().top;
    console.log(poci1);
    let tamañopantalla = window.innerHeight/1;

    if(poci1 < tamañopantalla)
    {
        animacion.style.animation = 'mover 3s ease-out'
    }

});
window.addEventListener('scroll', function()
{
    let animacion = document.getElementById('animado5');
    let poci1 = animacion.getBoundingClientRect().top;
    console.log(poci1);
    let tamañopantalla = window.innerHeight/1;

    if(poci1 < tamañopantalla)
    {
        animacion.style.animation = 'mover 3s ease-out'
    }

});